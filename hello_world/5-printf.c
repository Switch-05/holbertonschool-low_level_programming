#include <stdio.h>
/**
 * main - This program prints exactly a text followed by a new line.
 *
 * Description: You cant use puts, insted use printf.
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
